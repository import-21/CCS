[project]
name = "claude-context-server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.0.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["indexer*", "storage*"]

[project.scripts]
context-server = "server:mcp.run"
